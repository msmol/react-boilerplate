# React Boilerplate

A simple boilerplate React application, built with esbuild

## Running

Run both `npm run build:client:watch` and `npm run start:dev` in a terminal to build both client and server respectively. Access at http://localhost:3000
